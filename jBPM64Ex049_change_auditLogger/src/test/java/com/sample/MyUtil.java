package com.sample;

import org.kie.api.runtime.KieSession;
import org.kie.api.runtime.process.ProcessContext;
import org.kie.internal.runtime.manager.InternalRuntimeManager;

public class MyUtil {

    public static void changeAuditLogger(ProcessContext kcontext) {
//        KieSession kruntime = (KieSession)kcontext.getKieRuntime();
//        InternalRuntimeManager runtimeManager = (InternalRuntimeManager) kruntime.getEnvironment().get("RuntimeManager");
//        System.out.println(runtimeManager);
        
//        runtimeManager.getEnvironment().
    }
}
